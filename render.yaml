services:
  - type: web
    name: sharepoint-flask-app
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envGroups:
      - flask-email
